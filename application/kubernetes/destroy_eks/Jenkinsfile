#!groovy

library 'kentrikos-shared-library'
pipeline {
    agent any
    environment {
        ENV_K8S_DIR = 'env-eks'
        OPERTION_DIR = "application/$AWS_REGION/$ENV_K8S_DIR"
    }
    agent any
    stages {
        stage('Git clone') {
            steps {
                gitCloneConfigRepo()
            }
        }
        stage('Init') {
            steps {
                terraformInitApplication dir: "$OPERTION_DIR", subcomponent: "${ENV_K8S_DIR}", withAWS: true
            }
        }
        stage('Plan') {
            steps {
                terraformPlanDestroy dir: "$OPERTION_DIR", withAWS: true
            }
        }
        stage('Apply') {
            input {
                message "Should we continue?"
                ok "Yes, we should."
            }
            steps {
                terraformApplyDestroy dir: "$OPERTION_DIR", withAWS: true
            }
        }
    }
}
