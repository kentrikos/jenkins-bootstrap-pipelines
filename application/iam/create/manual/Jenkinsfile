#!groovy

library 'kentrikos-shared-library'
pipeline {

    environment {
        OPERTION_DIR = "application/$AWS_REGION/iam/manual"
        SUBCOMPONENT = "iam"
    }
    agent any
    stages {
        stage('Git clone') {
            steps {
                gitCloneConfigRepo()
            }
        }
        stage('Init') {
            steps {
                terraformInitApplication dir: "$OPERTION_DIR", subcomponent: "$SUBCOMPONENT", withAWS: false
            }
        }
        stage('Plan') {
            steps {
                terraformPlan dir: "$OPERTION_DIR"
            }
        }
        stage('Apply') {
            input {
                message "Should we continue?"
                ok "Yes, we should."
            }
            steps {
                terraformApply dir: "$OPERTION_DIR"
            }
        }
    }
}
